---
label: Gallery
hide_body: true
fields:
- name: gallery
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
    description: The title or short description of the image.
  - name: photo
    type: file
    config:
      maxSize: 250
    label: Photo
    description: The photo.
  config:
    min: 
    max: 
    labelField: 
  label: Gallery
pages:
- gallery.md
- gallery.md
